<?php
/**
 * ============================================================================
 * КОНФИГУРАЦИЯ БЭКЕНДА — КИРИЛЛ ЧУМАК
 * ============================================================================
 * Настройки подключения к MAX Bot API, Yandex Disk REST API и локального логирования.
 */

// Запрет прямого вызова файла
if (!defined('APP_INIT') && basename($_SERVER['PHP_SELF']) === basename(__FILE__)) {
    header('HTTP/1.0 403 Forbidden');
    exit('Direct access forbidden.');
}

return [
    // ------------------------------------------------------------------------
    // 1. МЕССЕНДЖЕР MAX (MAX Bot API)
    // ------------------------------------------------------------------------
    'max' => [
        // Токен бота, полученный на business.max.ru в разделе «Чат-боты» -> «Настроить»
        'bot_token' => 'YOUR_MAX_BOT_TOKEN_HERE',
        
        // ID чата или пользователя для получения уведомлений о новых брифах
        // Если отправляете себе в ЛС — укажите user_id, если в чат/канал — chat_id
        'chat_id'   => 'YOUR_MAX_CHAT_ID_OR_USER_ID_HERE',
        'is_chat'   => true, // true, если отправка в chat_id; false, если в user_id
        
        // Базовый домен API MAX (актуальный шлюз: platform-api2.max.ru)
        'api_url'   => 'https://platform-api2.max.ru',
    ],

    // ------------------------------------------------------------------------
    // 2. ЯНДЕКС ДИСК / ЯНДЕКС ДОКУМЕНТЫ (Yandex Disk REST API)
    // ------------------------------------------------------------------------
    'yandex' => [
        // OAuth-токен от приложения Яндекс ID (права: Доступ к Диску)
        'oauth_token' => 'YOUR_YANDEX_OAUTH_TOKEN_HERE',
        
        // Путь к файлу таблицы брифов на Яндекс Диске (в папке приложения или корне)
        // Например: 'app:/Брифы_Клиентов.xlsx' или 'disk:/Брифы_Клиентов.xlsx'
        'briefs_file'   => 'app:/Брифы_Клиентов.xlsx',
        
        // Путь к файлу таблицы расписания и бронирования
        'schedule_file' => 'app:/Расписание_и_Записи.xlsx',
        
        // Время кеширования расписания в секундах (чтобы не дергать Диск на каждый хит)
        'cache_ttl'     => 120, // 2 минуты
    ],

    // ------------------------------------------------------------------------
    // 3. БЕЗОПАСНОСТЬ И АНТИСПАМ
    // ------------------------------------------------------------------------
    'security' => [
        // Часовой пояс
        'timezone'      => 'Europe/Moscow',
        
        // Включить локальное резервное логирование всех брифов в JSON
        'backup_logs'   => true,
        'logs_dir'      => __DIR__ . '/logs',
        'temp_dir'      => __DIR__ . '/temp',
        
        // Разрешенные домены для CORS (оставьте '*' или укажите ваш домен)
        'allowed_origin'=> '*',
    ]
];
